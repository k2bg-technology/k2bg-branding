name: 'Chromatic'

on: push

jobs:
  chromatic:
    name: Run Chromatic
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install pnpm
        uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install
      - name: Run Design System Chromatic
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.DESIGN_SYSTEM_CHROMATIC_PROJECT_TOKEN }}
          workingDir: packages/ui
          onlyChanged: true
          exitZeroOnChanges: true
      - name: Run Portfolio Chromatic
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.PORTFOLIO_CHROMATIC_PROJECT_TOKEN }}
          workingDir: apps/portfolio
          onlyChanged: true
          exitZeroOnChanges: true
      - name: Run Blog Chromatic
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.BLOG_CHROMATIC_PROJECT_TOKEN }}
          workingDir: apps/blog
          onlyChanged: true
          exitZeroOnChanges: true
