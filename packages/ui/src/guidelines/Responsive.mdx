import { Meta, Story, Unstyled } from '@storybook/addon-docs';
import { useTranslation } from 'react-i18next';
import '../docs.css';

import i18n from '../i18n.ts';

import Responsive from '../assets/responsive.png';

{(() => {
useTranslation();
})()}

<Unstyled>
<main className='grid gap-12'>

<section className='grid gap-4'>
  # Responsive
  <div className='flex flex-col gap-condensed'>
    <p>
      さまざまな画面サイズで快適に利用できるUIを目指し、モダンでありながら親しみやすいデザインを提供します。最新のデバイスでも使いやすく、かつ温かみのあるUI体験を提供することを目標としています。画面サイズに応じて柔軟にレイアウトを変化させつつ、アクセシビリティ（WCAG 2.1 AA以上）を常に意識して実装します。
    </p>
    <p>
      各コンポーネントは画面サイズに合わせて柔軟に拡張・縮小できるよう設計し、基本的なレイアウトを保ちながら要素を配置しやすい構造を意識します。画面が小さくなっても暗く見づらくならないよう、適切な色使いと余白を保ち、メインコンテンツを見やすく表示するためシンプルなレイアウトを心がけます。
    </p>
  </div>

<div>
  <img
    src={Responsive}
    alt="An abstraction representing the composition of data for a component"
  />
</div>

</section>

<section className="flex flex-col gap-spacious">
  ## ブレイクポイント

このデザインシステムでは、下記を目安にした主要ブレイクポイントを設定します。画面サイズが小さいほど、一列に表示する要素を減らして縦並び中心に。画面サイズが大きいほど、複数カラムを活用して統一感のあるレイアウトを出せるようにします。

| デバイスサイズ | 推奨ブレイクポイント | 主な用途・イメージ                                                         |
| -------------- | -------------------- | -------------------------------------------------------------------------- |
| Small          | 40rem(640px)         | スマートフォン（縦画面）想定。必要最低限の要素で操作をしやすく。           |
| Medium         | 48rem(768px)         | 小型タブレットや大きめスマホを想定。グリッド幅をやや増やす。               |
| Large          | 64rem(1024px)        | 横向きタブレット、または小型 PC 画面を想定。2〜3 カラムレイアウトへ。      |
| X-Large        | 80rem(1280px)        | ノート PC から一般的なデスクトップ想定。グリッド幅をさらに拡張。           |
| XX-Large       | 96rem(1536px)        | 大画面デスクトップや TV 表示を想定。余白や装飾アイテムにゆとりを持たせる。 |

</section>

<section className='flex flex-col gap-spacious'>
## レスポンシブのガイドライン

<div className="grid grid-cols-2 gap-6">
  {(() => {
      const items = [
      {
        "gradient": "linear-gradient(120deg, #84FAB0, #8FD3F4)",
        "heading": "デザインの一貫性とアクセシビリティ",
        "paragraphs": [
          "小さな画面でも、素材感のある色やアイコンを用いて温かみのあるデザインを演出します。デザインの特徴を損なわないよう、適切に要素を省略・再配置し、全体の統一感を保ちます。",
          "ビューの切り替えや折り返しが発生したときに、文字サイズやタップターゲットが極端に小さくならないよう考慮し、メディアクエリによるレイアウト変更時もフォーカス移動や読み上げ順序が崩れない設計を行います。"
        ]
      },
      {
        "gradient": "linear-gradient(90deg, #FEE140, #FA709A)",
        "heading": "グリッドシステムの設計",
        "paragraphs": [
          "スマートフォン向けのSmallでは4列を基本とし要素配置をシンプルに、タブレット向けのMediumでは6列、PC向けのLarge以上では12列を基本としアイテム配置の自由度を上げます。",
          "小さな画面では余白を詰め込みすぎないように注意しつつタップしやすい最小サイズ（約44px四方）を確保し、大きな画面では要素間の視認性を高めるため十分なマージンを設定します。",
          "基本的には左上から読み進める流れを保ちながら整理された配置を心がけ、大きなヒーローイメージやカードなどはカラムを跨いで印象的に表示します。"
        ]
      },
      {
        "gradient": "linear-gradient(45deg, #F6D365, #FDA085)",
        "heading": "ナビゲーションの実装",
        "paragraphs": [
          "Smallではグローバルナビゲーションをハンバーガーメニューに格納し、アクセシビリティに配慮してaria-label属性やaria-expanded属性を適切に設定します。",
          "Medium以上の場合はヘッダー部分にナビゲーション項目を並べるか、サイドバーを固定表示し、アイコンとテキストを組み合わせて視認性を確保します。"
        ]
      },
      {
        "gradient": "linear-gradient(135deg, #667eea, #764ba2)",
        "heading": "画像と装飾要素",
        "paragraphs": [
          "小さな画面では装飾用背景画像がテキストを覆わないよう工夫し、装飾としてのイラストにはalt=\"\"を設定します。",
          "画面サイズに応じて最適な画像を提供し、装飾要素はUIコンポーネントと干渉しないよう注意します。",
          "コントラスト比を担保し、アクセシビリティを損なわないよう画像の背後にオーバーレイを敷くなどの工夫を行います。"
        ]
      },
      {
        "gradient": "linear-gradient(135deg, #70F570, #49C628)",
        "heading": "タイポグラフィとフォーム要素",
        "paragraphs": [
          "レスポンシブ環境下で文字サイズを適切に変更し、行間や文字間隔を自動調整できるようにします。",
          "背景色使用時は4.5:1以上のコントラスト比を確保し、小さな画面での文章折り返しにも配慮します。",
          "フォームは画面サイズに応じて最適なレイアウトを選択し、エラー表示は視認性とアクセシビリティに配慮して実装します。"
        ]
      },
      {
        "gradient": "linear-gradient(135deg, #B2FEFA, #0ED2F7)",
        "heading": "コンポーネント実装のポイント",
        "paragraphs": [
          "カルーセル、モーダル、通知など各コンポーネントは画面サイズに応じて適切な表示方法を選択し、アクセシビリティ対応を確実に行います。",
          "メディアクエリによる段階的な要素制御、アニメーションの最適化、タッチ・マウス操作の考慮、そしてaria属性の適切な管理を通じて、すべての利用者に配慮したUIを実現します。"
        ]
      }
    ];

    return items.map((item, index) => (
      <div key={index} className="flex flex-col gap-spacious">
        <div className="w-full h-[200px] rounded-lg relative overflow-hidden">
          <div
            className="absolute inset-0"
            style={{
              background: item.gradient,
            }}
          />

          <div
            className="absolute inset-0 mix-blend-overlay opacity-50"
            style={{
              backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E")`,
            }}
          />
        </div>
        <div className="flex flex-col gap-1">
          <h3>{item.heading}</h3>
          <div className="flex flex-col gap-condensed">
            {item.paragraphs.map((text, pIndex) => (
              <p key={pIndex}>
                {text}
              </p>
            ))}
          </div>
        </div>
      </div>
    ));

})()}

</div>

</section>

</main>
</Unstyled>
