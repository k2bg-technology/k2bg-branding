import { Meta, Story, Unstyled } from '@storybook/addon-docs';
import { useTranslation } from 'react-i18next';
import '../docs.css';

import i18n from '../i18n.ts';

import Responsive from '../assets/responsive.png';

{(() => {
useTranslation();
})()}

<Unstyled>
<main className="grid gap-12">

  <section className="grid gap-4">
    # {i18n.t('responsive.title')}
    <div className="flex flex-col gap-condensed">
      <p>{i18n.t('responsive.description.paragraph1')}</p>
      <p>{i18n.t('responsive.description.paragraph2')}</p>
    </div>

    <div>
      <img
        src={Responsive}
        alt={i18n.t('responsive.heroImageAlt')}
      />
    </div>

  </section>

  <section className="flex flex-col gap-spacious">
    ## {i18n.t('responsive.breakpoints.heading')}
    <p>{i18n.t('responsive.breakpoints.description')}</p>

| {i18n.t('responsive.breakpoints.table.deviceSize')} | {i18n.t('responsive.breakpoints.table.breakpoint')} | {i18n.t('responsive.breakpoints.table.usage')} |
| --------------------------------------------------- | --------------------------------------------------- | ---------------------------------------------- |
| {i18n.t('responsive.breakpoints.row1.device')}      | 40rem(640px)                                        | {i18n.t('responsive.breakpoints.row1.usage')}  |
| {i18n.t('responsive.breakpoints.row2.device')}      | 48rem(768px)                                        | {i18n.t('responsive.breakpoints.row2.usage')}  |
| {i18n.t('responsive.breakpoints.row3.device')}      | 64rem(1024px)                                       | {i18n.t('responsive.breakpoints.row3.usage')}  |
| {i18n.t('responsive.breakpoints.row4.device')}      | 80rem(1280px)                                       | {i18n.t('responsive.breakpoints.row4.usage')}  |
| {i18n.t('responsive.breakpoints.row5.device')}      | 96rem(1536px)                                       | {i18n.t('responsive.breakpoints.row5.usage')}  |

  </section>

  <section className="flex flex-col gap-spacious">
    ## {i18n.t('responsive.guidelines.heading')}

    <div className="grid grid-cols-2 gap-6">
    {(() => {
      const gradients = [
        "linear-gradient(73deg, #ff9a9e, #fad0c4)",
        "linear-gradient(112deg, #a1c4fd, #c2e9fb)",
        "linear-gradient(87deg, #d4fc79, #96e6a1)",
        "linear-gradient(45deg, #84fab0, #8fd3f4)",
        "linear-gradient(158deg, #fccb90, #d57eeb)",
        "linear-gradient(120deg, #ffb347 0%, #ffcc33 100%)",
      ];

      const items = i18n.t('responsive.guidelines.items', { returnObjects: true });

      return Object.values(items).map((item, index) => (
        <div key={index} className="flex flex-col gap-spacious">
          <div className="w-full h-[200px] rounded-lg relative overflow-hidden">
            <div
              className="absolute inset-0"
              style={{
                background: gradients[index % gradients.length],
              }}
            />
            <div
              className="absolute inset-0 mix-blend-overlay opacity-50"
              style={{
              backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E")`,
              }}
            />
          </div>
          <div className="flex flex-col gap-1">
            <h3>{item.heading}</h3>
            {item.paragraphs.map((paragraph, index) => (
              <p key={index}>{paragraph}</p>
            ))}
          </div>
        </div>
      ));
    })()}
    </div>

  </section>

</main>
</Unstyled>
