import { Meta, Story, Unstyled } from '@storybook/addon-docs';
import { useTranslation } from 'react-i18next';
import '../docs.css';

import i18n from '../i18n.ts';

import Accessibility from '../assets/accessibility.png';
import { ScrollArea } from '../components/ScrollArea';

{(() => {
useTranslation();
})()}

<Unstyled>
<main className='grid gap-12'>

<section className='grid gap-4'>
    # {i18n.t('accessibility.title')}
    <p>{i18n.t('accessibility.intro')}</p>

    <div>
      <img
        src={Accessibility}
        alt={i18n.t('accessibility.heroImageAlt')}
      />
    </div>

  </section>

<section className='flex flex-col gap-spacious'>

## {i18n.t('accessibility.principles.heading')}

{(() => {
const sections = i18n.t('accessibility.principles.items', { returnObjects: true });
return sections.map((section, index) => (

<div key={index} className="flex flex-col gap-condensed">
  <h3>{section.title}</h3>
  <div className="flex flex-col gap-condensed">
    {section.paragraphs.map((paragraph, pIndex) => (
      <p key={pIndex}>{paragraph}</p>
    ))}
  </div>
</div>
)); })()}

</section>

<section className='flex flex-col gap-spacious'>
## {i18n.t('accessibility.guidelines.heading')}

<div className="grid grid-cols-2 gap-6">
  {(() => {
      const items = i18n.t('accessibility.guidelines.items', { returnObjects: true });
      const gradients = [
        "linear-gradient(73deg, #96fbc4 0%, #f9f586 100%)",
        "linear-gradient(45deg, #ff7e5f 0%, #feb47b 100%)",
        "linear-gradient(82deg, #4facfe 0%, #00f2fe 100%)",
        "linear-gradient(37deg, #43e97b 0%, #38f9d7 100%)",
        "linear-gradient(59deg, #fa709a 0%, #fee140 100%)",
        "linear-gradient(66deg, #7F7FD5 0%, #86A8E7 50%, #91EAE4 100%)",
        "linear-gradient(27deg, #f78ca0 0%, #f9748f 50%, #fd868c 100%)",
        "linear-gradient(90deg, #a18cd1 0%, #fbc2eb 100%)",
        "linear-gradient(33deg, #fad0c4 0%, #ffd1ff 100%)",
        "linear-gradient(120deg, #ffb347 0%, #ffcc33 100%)",
        "linear-gradient(54deg, #43cea2 0%, #185a9d 100%)",
        "linear-gradient(63deg, #cfd9df 0%, #e2ebf0 100%)",
        "linear-gradient(77deg, #ff9a9e 0%, #fad0c4 99%, #fad0c4 100%)",
        "linear-gradient(44deg, #a1c4fd 0%, #c2e9fb 100%)",
        "linear-gradient(95deg, #d4fc79 0%, #96e6a1 100%)",
        "linear-gradient(88deg, #84fab0 0%, #8fd3f4 100%)",
        "linear-gradient(120deg, #cfd9df 0%, #e2ebf0 100%)",
        "linear-gradient(67deg, #fccb90 0%, #d57eeb 100%)"
      ];

      return items.map((item, index) => (
        <div key={index} className="flex flex-col gap-spacious">
          <div className="w-full h-[200px] rounded-lg relative overflow-hidden">
            <div
              className="absolute inset-0"
              style={{
                background: gradients[index % gradients.length],
              }}
            />

            <div
              className="absolute inset-0 mix-blend-overlay opacity-50"
              style={{
                backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E")`,
              }}
            />
          </div>
          <div className="flex flex-col gap-1">
            <h3>{item.heading}</h3>
            <p>{item.paragraph}</p>
          </div>
        </div>
      ));

})()}

</div>

</section>
</main>
</Unstyled>
