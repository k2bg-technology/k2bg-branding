import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import color from 'tailwind-config/design-token/color.json';

# Color

<ColorPalette>
  {
    Object.keys(color).map((key) => (
      <ColorItem
        key={key}
        title={color[key].customVariable}
        subtitle={color[key].description}
        colors={[color[key].value]}
      />
    ))
  }
</ColorPalette>
