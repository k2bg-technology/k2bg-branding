import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import { Icon } from '../components/Icon';
import { MULTI_COLOR_ICON_NAMES, HERO_ICON_NAMES } from '../components/Icon/const';

# Icon

## Multi Color

<IconGallery style={{ marginTop: "30px"}}>
  {
    MULTI_COLOR_ICON_NAMES.map((name) => (
      <IconItem key={name} name={name}>
        <Icon name={name} originalColor width={20} height={20} />
      </IconItem>
    ))
  }
</IconGallery>

## Hero

<IconGallery style={{ marginTop: "30px"}}>
  {
    HERO_ICON_NAMES.map((name) => (
      <IconItem key={name} name={name}>
        <Icon name={name} width={20} height={20} />
      </IconItem>
    ))
  }
</IconGallery>
