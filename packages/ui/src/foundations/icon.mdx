import {
  Meta,
  IconGallery,
  IconItem,
  Unstyled,
} from '@storybook/addon-docs/blocks';
import { Icon } from '../components/Icon';
import {
  MULTI_COLOR_ICON_NAMES,
  HERO_ICON_NAMES,
} from '../components/Icon/const';
import '../docs.css';

<Unstyled>
<main className='grid gap-12'>
# Icon

<section className='flex flex-col gap-spacious'>
## Multi Color

<IconGallery>
  {MULTI_COLOR_ICON_NAMES.map((name) => (
    <IconItem key={name} name={name}>
      <Icon name={name} originalColor width={20} height={20} />
    </IconItem>
  ))}
</IconGallery>
</section>

<section className='flex flex-col gap-spacious'>
## Hero

<IconGallery>
  {HERO_ICON_NAMES.map((name) => (
    <IconItem key={name} name={name}>
      <Icon name={name} width={20} height={20} />
    </IconItem>
  ))}
</IconGallery>
</section>
</main>
</Unstyled>
