/**
 * Default values used across adapters when source data is missing.
 */
export const DEFAULT_VALUES = {
  PLACEHOLDER_IMAGE_URL: 'https://placeholder.com/affiliate.jpg',
  DEFAULT_PROVIDER_COLOR: '#000000',
  DEFAULT_IMAGE_WIDTH: 200,
  DEFAULT_IMAGE_HEIGHT: 200,
} as const;
